# Project Name

## Description
An API that return the response from Gemini.

## Prerequisites
- Node.js (version 18 or higher)
- npm (Node Package Manager)

## Installation
1. Clone the repository
2. Navigate to the project server directory
3. Install dependencies: `npm install`

## Configuration
1. Rename `.env.example` file to `.env`.
2. Update the necessary environment variables in the `.env` file.

## Usage
1. Start the server: `npm start` or `nodemon app.js`(nodemon installation required)
2. Use postman to fetch api
